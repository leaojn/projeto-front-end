import styled, { css } from 'styled-components';

export const Container = styled.div`
  ${({ theme }) => css``}
`;

export const ContainerMarginMenu = styled.div`
  ${({ theme }) => css`
    margin-top: 5rem;
  `}
`;
